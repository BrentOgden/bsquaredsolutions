[build]
  command = "npx puppeteer browsers install chrome && vite build && node scripts/prerender-meta.mjs"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[dev]
  targetPort = 5173
  port = 8888
